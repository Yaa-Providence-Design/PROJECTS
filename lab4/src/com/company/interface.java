package com.company;

class Product{
    
package com.company;

    public interface inter {
        interface productl{
            void add(String item);
        }
        class productImpl implements productl {
            public void add() {

            }

            @Override
            public void add(String product) {
                void addNewProduct(product product){
                    try{
                        if
                    }
                }

            }
        }

    }






/*interface ProductPricingService<T> {
    double priceStock = 0;
    String exchange =null;
    String ticker = null;
    double priceFuture= 0;
    String exchange = null;
    String contractCode= null;
    int month= Integer.parseInt(null);
    int year= Integer.parseInt(null);
    void set(double priceStock);
}
public class interface1 implements ProductPricingService {
    public static void main(String[] args) {
        System.out.println(priceStock);
    }

    @Override
    public void se() {
        System.out.println("price cannot be negative");
    }

    @Override
    public void set(double priceStock) {

    }
}